version: "2"
sql:
  - engine: "postgresql"
    queries: "./db/queries"
    schema: "./migrations"
    gen:
      go:
        package: "db"
        out: "./db"
        emit_json_tags: true
        emit_db_tags: true
        emit_prepared_queries: false
        emit_interface: true
        emit_exact_table_names: false
        sql_package: "pgx/v5"
